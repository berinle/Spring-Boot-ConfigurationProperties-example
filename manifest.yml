---
applications:
- name: config-sample
  memory: 1gb
  buildpacks:
    - java_buildpack_offline
  path: target/configuration-properties-example-0.0.1-SNAPSHOT.jar
  env:
    APP_COMPILER_TIMEOUT: 100
    APP_COMPILER_OUTPUTFOLDER: /tmp
    APP_MENUS[0]_TITLE: Welcome
